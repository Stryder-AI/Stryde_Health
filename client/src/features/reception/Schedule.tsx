import { AppointmentCalendar } from '@/features/shared/AppointmentCalendar';

export default function ReceptionSchedule() {
  return (
    <AppointmentCalendar
      title="Appointment Schedule"
      subtitle="All doctors · Full hospital schedule"
    />
  );
}
